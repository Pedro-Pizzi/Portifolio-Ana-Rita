<section id="formacao" class="container">
  <div class="section-title">
    <h2>Formação contínua<br /><span class="italic-serif">organizada com clareza.</span></h2>
    <p>Minha formação une educação, inclusão, aprendizagem e neurodesenvolvimento. Abaixo, separei o que já foi concluído do que está em andamento.</p>
  </div>

  <div class="training-grid">
    <article class="train-card solid-mint">
      <h3>Concluídas</h3>
      <ul>
        <li>Pedagogia <small>FAIBI Ibitinga</small></li>
        <li>Psicopedagogia: atuação em contextos educativos <small>PUC Minas Gerais</small></li>
        <li>Psicologia Escolar</li>
        <li>Dificuldades e Transtornos de Aprendizagem</li>
        <li>Leitura, Escrita e Produção de Texto</li>
        <li>Gestão e Organização da Escola</li>
      </ul>
    </article>

    <article class="train-card solid-butter">
      <h3>Em andamento</h3>
      <ul>
        <li>Pós-graduação em Análise do Comportamento Aplicada ao TEA</li>
        <li>Pós-graduação em Neurociências e Autismo</li>
      </ul>
      <div class="note-box">
        <strong>Cursos complementares</strong>
        ABA, TEA na escola, alfabetização de crianças típicas e atípicas, Congresso Espectro, simpósios sobre autismo, inclusão e neurodesenvolvimento.
      </div>
    </article>
  </div>
</section>

<style>
  .section-title { max-width: 46rem; margin: 0 auto clamp(36px, 6vw, 64px); text-align: center; }
  .section-title h2 { margin-bottom: 20px; }
  .training-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: clamp(20px, 3vw, 32px); }
  .train-card { padding: clamp(28px, 5vw, 52px); border: 1px solid var(--line-color); border-radius: var(--radius-lg); }
  .solid-mint { background: color-mix(in srgb, var(--accent-mint) 42%, var(--bg-primary)); }
  .solid-butter { background: color-mix(in srgb, var(--accent-butter) 50%, var(--bg-primary)); }
  .train-card h3 { margin-bottom: 24px; padding-bottom: 16px; border-bottom: 1px solid var(--line-color); font-size: 2rem; }
  ul { display: grid; gap: 18px; list-style: none; }
  li { color: var(--text-ink); font-size: 1.05rem; font-weight: 500; }
  li small { display: block; margin-top: 4px; color: var(--text-muted); font-size: 0.76rem; font-weight: 600; letter-spacing: 0.05em; text-transform: uppercase; }
  .note-box { margin-top: 32px; padding-top: 22px; border-top: 1px solid color-mix(in srgb, var(--brand-gold) 55%, transparent); color: var(--text-muted); font-size: 0.95rem; }
  .note-box strong { display: block; margin-bottom: 6px; color: var(--brand-purple); font-family: var(--font-serif); font-size: 1.15rem; }
  @media (max-width: 800px) { .training-grid { grid-template-columns: 1fr; } }
</style>
