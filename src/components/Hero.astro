<section id="hero" class="hero-section container">
  <div class="hero-grid">
    <div class="hero-content">
      <h1 class="hero-title reveal-up">
        <span>Inclusão no</span>
        <span>aprender, brincar</span>
        <span class="italic-serif">e no viver 🧠</span>
      </h1>
      <p class="reveal-up stagger-1">Sou Ana Rita Marques, criadora de conteúdo digital e pedagoga inclusiva. Minha missão é construir rotinas e estratégias reais para cada criança ✨, focando em materiais concretos e regulação sensorial.</p>

      <div class="actions reveal-up stagger-2">
        <a class="btn btn-primary interactive" href="#sobre">Minha Trajetória</a>
        <a class="btn btn-glass interactive" href="#projetos">Meus Links e Projetos 👇🏽</a>
      </div>
    </div>

    <div class="hero-visual reveal-up stagger-3">
      <svg class="morphing-blob" viewBox="0 0 200 200" aria-hidden="true" preserveAspectRatio="none">
        <path fill="var(--brand-purple-light)" opacity="0.15">
          <animate 
            attributeName="d" 
            dur="12s" 
            repeatCount="indefinite"
            values="
              M 100, 10 C 150, 10 190, 50 190, 100 C 190, 150 150, 190 100, 190 C 50, 190 10, 150 10, 100 C 10, 50 50, 10 100, 10 Z;
              M 100, 20 C 160, 10 180, 60 180, 100 C 180, 140 140, 180 100, 180 C 60, 180 20, 140 20, 100 C 20, 60 40, 10 100, 20 Z;
              M 100, 10 C 150, 20 190, 40 190, 100 C 190, 160 160, 190 100, 190 C 40, 190 10, 160 10, 100 C 10, 40 50, 20 100, 10 Z;
              M 100, 10 C 150, 10 190, 50 190, 100 C 190, 150 150, 190 100, 190 C 50, 190 10, 150 10, 100 C 10, 50 50, 10 100, 10 Z
            "
          />
        </path>
        <path fill="none" stroke="var(--brand-gold)" stroke-width="0.8" stroke-dasharray="4 8">
          <animate 
            attributeName="d" 
            dur="18s" 
            repeatCount="indefinite"
            values="
              M 100, 5 C 160, 5 195, 40 195, 100 C 195, 160 160, 195 100, 195 C 40, 195 5, 160 5, 100 C 5, 40 40, 5 100, 5 Z;
              M 100, 15 C 150, 0 200, 50 190, 100 C 180, 150 150, 200 100, 190 C 50, 180 0, 150 10, 100 C 20, 50 50, 30 100, 15 Z;
              M 100, 5 C 160, 5 195, 40 195, 100 C 195, 160 160, 195 100, 195 C 40, 195 5, 160 5, 100 C 5, 40 40, 5 100, 5 Z
            "
          />
        </path>
      </svg>

      <div class="portrait-wrapper">
        <img src="/images/ig_2.jpg" alt="Ana Rita Marques — Inclusão e Aprendizagem" width="800" height="1000" fetchpriority="high" />
      </div>

      <div class="badge glass-panel interactive">
        <span class="badge-num">10+</span>
        <span class="badge-text">Anos de<br />prática</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    min-height: min(820px, calc(100vh - 76px));
    display: flex;
    align-items: center;
    padding-top: clamp(40px, 7vw, 88px);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(320px, 0.75fr);
    gap: clamp(32px, 5vw, 72px);
    align-items: center;
  }

  .hero-title span {
    display: block;
    padding-bottom: 4px;
  }

  .hero-content p {
    max-width: 39rem;
    margin: 28px 0 36px;
    font-size: clamp(1.05rem, 1.6vw, 1.2rem);
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .hero-visual {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    display: grid;
    place-items: center;
    transform: perspective(1000px) rotateX(calc(var(--mouse-y, 0) * -4deg)) rotateY(calc(var(--mouse-x, 0) * 4deg));
    transition: transform 0.5s cubic-bezier(0.2, 1, 0.2, 1);
  }

  .portrait-wrapper {
    width: 76%;
    aspect-ratio: 4 / 5;
    position: relative;
    z-index: 2;
    overflow: hidden;
    border-radius: 24px 24px 0 24px;
  }

  .portrait-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .morphing-blob {
    position: absolute;
    inset: 50% auto auto 50%;
    pointer-events: none;
    transform: translate(-50%, -50%) scale(1.2);
    width: 120%;
    height: 120%;
    z-index: 1;
    filter: url(#ambient-blur);
    animation: rotateSlowly 40s linear infinite;
  }
  
  @keyframes rotateSlowly {
    from { transform: translate(-50%, -50%) scale(1.2) rotate(0deg); }
    to { transform: translate(-50%, -50%) scale(1.2) rotate(360deg); }
  }

  .badge {
    position: absolute;
    right: 0;
    bottom: 7%;
    z-index: 4;
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 18px 22px;
    border-radius: var(--radius-sm);
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .badge:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-sm);
  }

  .badge-num {
    color: var(--brand-gold);
    font-family: var(--font-serif);
    font-size: clamp(2rem, 3vw, 2.75rem);
    line-height: 1;
  }

  .badge-text {
    color: var(--text-ink);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    line-height: 1.35;
    text-transform: uppercase;
  }

  @media (max-width: 968px) {
    .hero-section { min-height: auto; padding-top: 56px; }
    .hero-grid { grid-template-columns: 1fr; gap: 40px; }
    .hero-title { font-size: clamp(2.5rem, 11vw, 3.25rem); }
    .hero-content p { max-width: 34rem; }
    .hero-visual { max-width: 460px; margin-inline: auto; }
  }

  @media (max-width: 480px) {
    .actions .btn { width: 100%; }
    .badge { right: 4%; bottom: 3%; padding: 14px 16px; }
  }
</style>
