<section id="hero" class="hero-section container">
  <div class="hero-grid">
    <div class="hero-content">
      <h1 class="hero-title">
        <span>O caminho</span>
        <span>da aprendizagem</span>
        <span class="italic-serif">com propósito.</span>
      </h1>
      <p>Sou Ana Rita Marques, pedagoga especialista em neurodesenvolvimento e inclusão. Minha missão é traduzir a complexidade da aprendizagem em práticas reais que acolhem e desenvolvem.</p>

      <div class="actions">
        <a class="btn btn-primary" href="#sobre">Conhecer Trajetória</a>
        <a class="btn btn-glass" href="#projetos">Projetos</a>
      </div>
    </div>

    <div class="hero-visual">
      <svg class="ring-dashed" viewBox="0 0 200 200" aria-hidden="true">
        <circle cx="100" cy="100" r="95" fill="none" stroke="var(--brand-gold)" stroke-width="1" stroke-dasharray="4 6" />
      </svg>
      <svg class="ring-solid" viewBox="0 0 200 200" aria-hidden="true">
        <circle cx="100" cy="100" r="85" fill="none" stroke="var(--brand-purple)" stroke-width="2" />
      </svg>

      <div class="portrait-wrapper">
        <img src="https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?auto=format&fit=crop&q=80&w=800&h=1000" alt="Ana Rita Marques — Pedagoga" width="800" height="1000" fetchpriority="high" />
      </div>

      <div class="badge">
        <span class="badge-num">10+</span>
        <span class="badge-text">Anos de<br />prática</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    min-height: min(820px, calc(100vh - 76px));
    display: flex;
    align-items: center;
    padding-top: clamp(40px, 7vw, 88px);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(320px, 0.75fr);
    gap: clamp(32px, 5vw, 72px);
    align-items: center;
  }

  .hero-title span {
    display: block;
    padding-bottom: 4px;
  }

  .hero-content p {
    max-width: 39rem;
    margin: 28px 0 36px;
    font-size: clamp(1.05rem, 1.6vw, 1.2rem);
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .hero-visual {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    display: grid;
    place-items: center;
  }

  .portrait-wrapper {
    width: 76%;
    aspect-ratio: 4 / 5;
    position: relative;
    z-index: 2;
    overflow: hidden;
    border-radius: 24px 24px 0 24px;
  }

  .portrait-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ring-dashed,
  .ring-solid {
    position: absolute;
    inset: 50% auto auto 50%;
    pointer-events: none;
    transform: translate(-50%, -50%);
  }

  .ring-dashed { width: 100%; height: 100%; z-index: 1; }
  .ring-solid { width: 108%; height: 108%; z-index: 3; }

  .badge {
    position: absolute;
    right: 0;
    bottom: 7%;
    z-index: 4;
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 18px 22px;
    background: var(--bg-primary);
    border: 1px solid var(--line-color);
    border-radius: var(--radius-sm);
  }

  .badge-num {
    color: var(--brand-gold);
    font-family: var(--font-serif);
    font-size: clamp(2rem, 3vw, 2.75rem);
    line-height: 1;
  }

  .badge-text {
    color: var(--text-ink);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    line-height: 1.35;
    text-transform: uppercase;
  }

  @media (max-width: 968px) {
    .hero-section { min-height: auto; padding-top: 56px; }
    .hero-grid { grid-template-columns: 1fr; gap: 40px; }
    .hero-title { font-size: clamp(2.5rem, 11vw, 3.25rem); }
    .hero-content p { max-width: 34rem; }
    .hero-visual { max-width: 460px; margin-inline: auto; }
  }

  @media (max-width: 480px) {
    .actions .btn { width: 100%; }
    .badge { right: 4%; bottom: 3%; padding: 14px 16px; }
  }
</style>
