<section id="sobre" class="container">
  <div class="about-chapter">
    <div class="about-grid">
      <figure class="photo-frame glass-panel reveal-up">
        <img src="/images/ig_3.jpg" alt="Ana Rita Marques atuando" width="700" height="900" loading="lazy" decoding="async" />
      </figure>

      <div class="about-content reveal-up stagger-1">
        <h2>Educação, inclusão e aprendizagem <span class="italic-serif">com propósito.</span></h2>
        <p>Sou Ana Rita Marques, pedagoga e criadora de conteúdo. Minha trajetória nasce da prática: do contato diário com crianças atípicas, famílias e os desafios reais da sala de aula e da clínica.</p>
        <p>Acredito que o brincar e os materiais concretos são as pontes para desenvolver funções executivas e construir um espaço lúdico e seguro.</p>
        <blockquote>“A inclusão acontece no aprender, no brincar e no viver. Estratégias reais para cada criança.”</blockquote>
      </div>
    </div>
  </div>

  <div class="services">
    <div class="section-title reveal-up">
      <h2>Uma prática que une mediação, adaptação e <span class="italic-serif">presença.</span></h2>
      <p>Meu trabalho envolve acompanhamento pedagógico, mediação da aprendizagem, adaptação de atividades e estratégias individualizadas.</p>
    </div>

    <div class="work-cards">
      <article class="work-card reveal-up stagger-1">
        <h3>Clínica Evolução</h3>
        <p>Atuação com práticas voltadas à aprendizagem, atenção, alfabetização, desenvolvimento e fortalecimento de habilidades pedagógicas.</p>
      </article>
      <article class="work-card reveal-up stagger-2">
        <h3>Rede Pública</h3>
        <p>Professora auxiliar, acompanhando a rotina escolar e contribuindo com estratégias para tornar a inclusão mais possível no dia a dia.</p>
      </article>
      <article class="work-card reveal-up stagger-3">
        <h3>Intervenção Lúdica</h3>
        <p>Jogos, materiais manipuláveis, recursos visuais e propostas lúdicas para favorecer participação, autonomia, atenção e aprendizagem.</p>
      </article>
    </div>
  </div>
</section>

<style>
  .about-chapter { padding-bottom: clamp(72px, 10vw, 120px); }
  .about-grid { display: grid; grid-template-columns: minmax(260px, 0.8fr) minmax(0, 1.2fr); align-items: center; gap: clamp(40px, 7vw, 88px); }
  .photo-frame { overflow: hidden; border-radius: var(--radius-lg) var(--radius-lg) 0 var(--radius-lg); aspect-ratio: 3 / 4; background: var(--bg-secondary); }
  .photo-frame img { width: 100%; height: 100%; object-fit: cover; }
  .about-content h2 { margin-bottom: 24px; }
  .about-content p + p { margin-top: 18px; }
  blockquote { margin-top: 32px; max-width: 38rem; color: var(--brand-purple); font-family: var(--font-serif); font-size: clamp(1.3rem, 2.2vw, 1.65rem); font-style: italic; line-height: 1.4; }
  .section-title { max-width: 44rem; margin: 0 auto clamp(36px, 6vw, 64px); text-align: center; }
  .section-title h2 { margin-bottom: 18px; }
  .work-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 20px; }
  .work-card { min-height: 100%; padding: clamp(24px, 3.5vw, 40px); border: 1px solid var(--line-color); border-radius: var(--radius-md); background: color-mix(in srgb, var(--bg-primary) 86%, white); }
  .work-card h3 { margin-bottom: 14px; color: var(--brand-purple); font-family: var(--font-sans); font-size: 1.2rem; }
  @media (max-width: 900px) { .about-grid { grid-template-columns: 1fr; } .photo-frame { max-width: 460px; } .work-cards { grid-template-columns: 1fr; } }
</style>
