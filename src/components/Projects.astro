<section id="projetos" class="container projects">
  <div class="project-grid">
    <div class="project-image glass-panel liquid-hover nebula-image reveal-up interactive">
      <img src="/images/ig_4.jpg" alt="Pessoa utilizando fones de ouvido em ambiente calmo" width="800" height="960" loading="lazy" decoding="async" />
    </div>
    <div class="content reveal-up stagger-1">
      <h2>Regulação Sensorial <span class="italic-serif">também é cuidado.</span></h2>
      <p><strong>Embaixadora Nebula Audio:</strong> Reduzir o ruído é prevenir sobrecargas. Seja em salas de aula ou eventos, os protetores auditivos garantem bem-estar tanto para pessoas atípicas quanto para nós, adultos.</p>
      <p class="coupon">Cupom: <strong>diariodeumaproinclusiva</strong></p>
      <a class="btn btn-primary" href="https://www.nebulaaudio.com.br/" target="_blank" rel="noopener">Acessar Nebula Audio</a>
    </div>
  </div>

  <div class="project-grid project-grid-reverse">
    <div class="content reveal-up">
      <h2>Sistema A.N.A. 📝</h2>
      <p><strong>Projeto idealizado:</strong> Saia apenas do papel e da caneta. O Sistema ANA é uma plataforma criada a partir da minha vivência para organizar relatórios, PEI e acompanhamentos de forma previsível e prática.</p>
      <p>Desenvolvido para criar rotinas reais que facilitam a vida do profissional da educação.</p>
      <a class="btn btn-primary interactive" href="https://anasistema.com.br" target="_blank" rel="noopener">Conhecer o Sistema</a>
    </div>
    <div class="system-preview reveal-up stagger-1" aria-label="Prévia visual do Sistema A.N.A.">
      <img class="phone-back" src="/images/sistema_ana_reports.png" alt="Tela de painel do Sistema ANA" width="480" height="960" loading="lazy" decoding="async" />
      <img class="phone-front" src="/images/sistema_ana_dashboard.png" alt="Tela de aplicativo do Sistema ANA" width="480" height="960" loading="lazy" decoding="async" />
    </div>
  </div>
</section>

<style>
  .projects { display: grid; gap: clamp(80px, 12vw, 152px); }
  .project-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); align-items: center; gap: clamp(40px, 7vw, 88px); }
  .content h2 { margin-bottom: 24px; font-size: clamp(2.5rem, 4.5vw, 4.5rem); }
  .content p { margin-bottom: 20px; font-size: 1.08rem; }
  .project-image { min-height: 560px; overflow: hidden; border-radius: var(--radius-lg) var(--radius-lg) 0 var(--radius-lg); background: var(--accent-mint); }
  .project-image img { width: 100%; height: 100%; object-fit: cover; }
  .coupon { display: inline-block; padding: 12px 16px; border-radius: var(--radius-sm); background: color-mix(in srgb, var(--accent-butter) 78%, white); color: var(--text-ink); }
  .coupon strong { color: var(--brand-purple); }
  .system-preview { position: relative; min-height: 560px; display: grid; place-items: center; isolation: isolate; }
  .system-preview::before { content: ''; position: absolute; inset: 8%; z-index: -1; border-radius: 50%; background: var(--accent-mint); }
  .system-preview img { grid-area: 1 / 1; width: min(48%, 260px); aspect-ratio: 1 / 2; object-fit: cover; border: 10px solid white; border-radius: 26px; box-shadow: 0 8px 20px rgba(43, 34, 48, 0.14); }
  .phone-back { transform: translateX(22%) rotate(6deg); z-index: 1; }
  .phone-front { transform: translateX(-22%) rotate(-5deg); z-index: 2; }
  @media (max-width: 900px) {
    .project-grid { grid-template-columns: 1fr; }
    .project-grid-reverse .content { order: 1; }
    .project-grid-reverse .system-preview { order: 2; }
    .project-image, .system-preview { min-height: 420px; }
  }
  @media (max-width: 480px) { .project-image, .system-preview { min-height: 340px; } }
</style>
